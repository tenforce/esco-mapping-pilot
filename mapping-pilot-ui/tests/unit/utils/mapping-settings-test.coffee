`import mappingSettings from '../../../utils/mapping-settings'`
`import { module, test } from 'qunit'`

module 'mappingSettings'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = mappingSettings()
  assert.ok result
